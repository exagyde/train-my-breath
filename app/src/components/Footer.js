"use client";

const STYLE = {
    display: "flex",
    flexDirection: "row",
    justifyContent: "center",
    padding: "20px 0"
};

export default function Footer() {
    return (
        <footer style={STYLE}>
            <small>©2026 Train My Breath</small>
        </footer>
    );
}